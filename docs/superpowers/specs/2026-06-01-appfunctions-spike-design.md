# AppFunctions Spike — Expose Billboard Hot 100 #1 to Gemini App

**Date:** 2026-06-01
**Status:** Design — awaiting user review
**Scope:** Test-only spike. Minimum viable surface to verify end-to-end that the Gemini app on a Galaxy S25 can invoke a `@AppFunction` exposed by the Billboard app and receive the current Hot 100 #1 song from our live Retrofit data — including from a cold-started app process.

## Goals (and Non-Goals)

**Goals**
- Expose exactly one `@AppFunction` (`getCurrentHot100TopSong`) from the Billboard app.
- Cold-boot DI works: when the system spawns our process to invoke the function, Hilt provides the existing `:core:domain` UseCase that ultimately calls Retrofit.
- Verify discovery (`adb shell cmd app_function list-app-functions`) and end-to-end invocation from the Gemini app (natural-language query → our function → Gemini-rendered answer).
- One PR, smallest possible diff.

**Non-Goals**
- No ADK integration in this app (Gemini app owns orchestration + Nano).
- No in-app chat UI / no `:feature:agent` module.
- No A2UI renderer.
- No multi-function surface (Top 10, search, collection mutations — all out of scope for the spike).
- No fallback for non-Android-16 devices in this spike — `minSdk` bump is accepted as a known break.

## Verified Facts (from spike research)

- `androidx.appfunctions:1.0.0-alpha09` (May 2026). Three artifacts:
  - `appfunctions` (client)
  - `appfunctions-service` (server-side — what we add)
  - `appfunctions-compiler` (KSP, code-gen)
- `AppFunctionManagerCompat` requires Android 16+ (API 36). Current project `minSdk = 32` → must bump to `36`.
- Provider interface is **`AppFunctionConfiguration.Provider`** (not `AppFunctionFactory.Provider` as the original guide claimed).
- KSP arg `appfunctions:aggregateAppFunctions = true` must be set once at the app module.
- Per-app-function permission for callers: `android.permission.EXECUTE_APP_FUNCTIONS` (Gemini app already holds it).
- Manifest registration is generated by the KSP compiler — no manual `<service>` / `<meta-data>` entry expected for this minimal case.
- Gemini app calling third-party AppFunctions is live in 2026 on Samsung OneUI 8.5+ (S25/S26) and Pixel 9+, but Google describes the developer ecosystem as "a small set of app developers" — automatic discovery by the Gemini app for arbitrary third-party functions is **not guaranteed**. This is the spike's primary unknown.

## Architecture

Single-PR layering, no new modules:

```
:app
  ├── BillboardApplication (existing @HiltAndroidApp)
  │     └─ NEW: implements AppFunctionConfiguration.Provider
  └── appfunctions/
        └─ BillboardFunctions.kt (NEW, @Inject constructor)
              └─ depends on existing :core:domain UseCase

:core:domain
  └── existing GetBillboardHot100UseCase (reused as-is — no new UseCase)
        signature: suspend operator fun invoke(): ChartOverview
        ChartOverview.chartList: List<Chart>  (Chart has rank/title/artist)

:core:data-impl / :core:data-source/prod
  └── existing ChartRepository + Retrofit (reused as-is)
```

Cold-boot flow (verified-by-design, not yet runtime-tested):

```
Gemini app  ──(AppSearch index)──▶  system AppFunction service
                                          │
                                          ▼
                              spawn billboard process (cold)
                                          │
                                          ▼
                       BillboardApplication.onCreate()  ← Hilt graph built
                                          │
                                          ▼
                     appFunctionConfiguration getter invoked
                                          │
                                          ▼
       AppFunctionConfiguration.Builder
         .addEnclosingClassFactory(BillboardFunctions::class.java) { billboardFunctions }
                                          │
                                          ▼
                    generated dispatcher calls BillboardFunctions.getCurrentHot100TopSong(...)
                                          │
                                          ▼
                          GetChartUseCase → ChartRepository → Retrofit
                                          │
                                          ▼
                       TopSong returned ──▶  Gemini app  ──▶  Nano renders answer
```

## Changes

### 1. `gradle/libs.versions.toml`
- Add `appfunctions = "1.0.0-alpha09"` version.
- Add three library entries: `androidx-appfunctions`, `androidx-appfunctions-service`, `androidx-appfunctions-compiler`.
- Bump `minSdk = "32"` → `"36"`.
- Bump `appVersionCode 7 → 8`, `appVersionName "1.2.0" → "1.3.0-spike"` (or whatever release flow prefers — to be decided at release time, not in spike PR).

### 2. `app/build.gradle.kts`
- Add KSP top-level arg: `ksp { arg("appfunctions:aggregateAppFunctions", "true") }`.
- Add three new dependencies (the `appfunctions-compiler` via `ksp(...)`).

### 3. `app/src/main/java/com/hancekim/billboard/BillboardApplication.kt`
- Implement `AppFunctionConfiguration.Provider`.
- Add `@Inject lateinit var billboardFunctions: BillboardFunctions`.
- Override `appFunctionConfiguration` to wire `BillboardFunctions` into the configuration.

### 4. `app/src/main/java/com/hancekim/billboard/appfunctions/BillboardFunctions.kt` (NEW)
- `class BillboardFunctions @Inject constructor(private val getBillboardHot100UseCase: GetBillboardHot100UseCase)`.
- `@AppFunctionSerializable data class TopSong(title, artist, rank)` with KDoc.
- `@AppFunction suspend fun getCurrentHot100TopSong(context: AppFunctionContext): TopSong`.
- Implementation: `getBillboardHot100UseCase().chartList.firstOrNull { it.rank == 1 }` → map to `TopSong`.
- Throws `AppFunctionElementNotFoundException` if no rank-1 entry (defensive — should not happen).

> Comments in this file will be written in Korean per project rule (`CLAUDE.md` language rules).

## Module-Boundary Note

The new file lives in `:app` (not in a feature module), so no `:core:domain` → `:core:data` boundary is crossed. `BillboardFunctions` injects only `GetChartUseCase` — same layering rule as Presenters.

## Verification Steps (post-merge, on Galaxy S25)

1. `./gradlew :app:assembleProdDebug`
2. Install to S25 (OneUI 8.5+ / Android 16).
3. `adb shell cmd app_function list-app-functions | grep com.hancekim.billboard`
   - **Pass:** the function id `getCurrentHot100TopSong` appears.
   - **Fail:** indexing did not happen — investigate KSP schema output + manifest.
4. Open Gemini app, ask: "What's the current #1 song on the Billboard Hot 100?"
   - **Pass:** Gemini answers with the live chart's #1 title + artist.
   - **Partial fail (function not discovered):** Gemini answers from its own training data. Investigate (a) whether Gemini app needs an explicit "connect" or category gating, (b) whether Google requires Play Console enrollment.
5. Force-stop the app, then repeat step 4 to verify cold-boot path.

## Known Risks

| Risk | Likelihood | Mitigation |
|---|---|---|
| Gemini app does not auto-discover third-party functions outside Google's allowlist | **Medium-high** — Google explicitly described it as a "small set of app developers" | If discovery fails, the spike's deliverable becomes a documented finding ("requires X enrollment") rather than a working demo. ADB list verification still proves our side is correct. |
| `androidx.appfunctions` alpha API changes before next quarter | Medium | Pin exact alpha09; revisit on each bump. |
| `minSdk = 36` breaks existing users | Certain | Accepted for spike branch; do **not** merge spike to `develop` unless we agree to ship the bump. Alternative: gate AppFunctions wiring behind a per-flavor build variant if we want to keep `prod` on `minSdk 32`. |
| Retrofit call hangs / fails during cold-boot invocation | Low | `runCatching` + `AppFunctionElementNotFoundException` on failure — fail fast so Gemini app surfaces the error. |
| KSP `aggregateAppFunctions` must be set at the *terminal* (app) module, not in convention plugin | Low | Place the `ksp { arg(...) }` block directly in `app/build.gradle.kts`, not in any convention plugin. |

## Open Questions (decide at code time, not now)

- Should `appfunctions-service` be added in a new `billboard.android.appfunctions` convention plugin, or inline in `app/build.gradle.kts`? **Recommendation: inline.** Convention plugins are for cross-module reuse; this is a one-shot in `:app`.
- Real minSdk floor for `appfunctions-service` may actually be 34 (per `AppFunctionManagerCompat U+` note) rather than 36 — the `/ai/appfunctions` page asserted 16+. Going with 36 to match the most recent doc claim; revisit if the alpha09 AAR manifest reveals a lower floor.

## Verification Results — Task 5 (ADB list-app-functions)

**Date:** 2026-06-01
**Device:** Galaxy S25, serial `R5CT40JH2HY`
**Install:** success — `Installed on 1 device.` (SM-S901N, Android 16)

**`adb shell cmd app_function list-app-functions`:**

```
No shell command implementation.
```

> Note: The `cmd app_function list-app-functions` sub-command is not exposed on OneUI 8.5 (Galaxy S25). The `app_function` service itself exists (`service list` confirms `android.app.appfunctions.IAppFunctionManager`). Verification was performed via `adb shell dumpsys app_function` and `adb shell dumpsys package com.hancekim.billboard` instead.

**`dumpsys package com.hancekim.billboard` — AppFunctionService entries:**

```
android.app.appfunctions.AppFunctionService:
  com.hancekim.billboard/androidx.appfunctions.service.PlatformAppFunctionService
    filter permission android.permission.BIND_APP_FUNCTION_SERVICE
    Action: "android.app.appfunctions.AppFunctionService"
  com.hancekim.billboard/androidx.appfunctions.service.ExtensionAppFunctionService
    filter permission android.permission.BIND_APP_FUNCTION_SERVICE
    Action: "android.app.appfunctions.AppFunctionService"
```

**`dumpsys app_function` — indexed AppFunctionMetadata for our package:**

```
AppFunctionMetadata for: com.hancekim.billboard.appfunctions.BillboardFunctions#getCurrentHot100TopSong
  Static Metadata:
    {
      "description": ["Returns the current #1 song on Billboard Hot 100."]
      "response": [{
        "valueType": [{
          "dataTypeReference": ["com.hancekim.billboard.appfunctions.BillboardFunctions$TopSong"]
          "type": [11]
          "isNullable": [false]
        }]
      }]
      "enabledByDefault": [true]
      "functionId": ["com.hancekim.billboard.appfunctions.BillboardFunctions#getCurrentHot100TopSong"]
      "mobileApplicationQualifiedId": ["android$apps-db/apps#com.hancekim.billboard"]
      "packageName": ["com.hancekim.billboard"]
    }
  Runtime Metadata:
    {
      "appFunctionStaticMetadataQualifiedId": ["android$apps-db/app_functions#com.hancekim.billboard/com.hancekim.billboard.appfunctions.BillboardFunctions#getCurrentHot100TopSong"]
      "functionId": ["com.hancekim.billboard.appfunctions.BillboardFunctions#getCurrentHot100TopSong"]
      "packageName": ["com.hancekim.billboard"]
    }
```

**Outcome:** ✅ function visible — both `PlatformAppFunctionService` and `ExtensionAppFunctionService` are registered, and the AppFunction system has indexed the full static + runtime metadata for `getCurrentHot100TopSong`. The `cmd app_function list-app-functions` shell sub-command not being implemented on Samsung OneUI is a device-specific CLI gap, not a registration failure.

---

## Out of Scope (explicitly)

- ADK Kotlin (`com.google.adk:google-adk-kotlin-core-android`) — not added.
- Gemini Nano via AICore — not added.
- A2UI renderer — not added.
- New `:feature:agent` module — not created.
- Additional `@AppFunction`s (collection mutations, multi-rank lookup, etc.) — separate future PRs.

---

## Final Outcome — 2026-06-01

### TL;DR

- Provider-side setup is **complete and matches the official `androidx.appfunctions` 1.0.0-alpha09 guide exactly.** No code change needed.
- Gemini app on Galaxy S25 did NOT invoke our function during home test. This is **NOT a bug in our setup.** It is a documented policy gate — see citation below.
- `EXECUTE_APP_FUNCTIONS` is a **caller-side** permission (Gemini, agent apps) — provider apps MUST NOT declare it. Earlier suspicion that we "forgot to register the permission" was incorrect.
- ADK is irrelevant to our app — ADK is for building agents, not for exposing functions. Our spike correctly excluded it.
- Computer Control is a separate framework (OEM-preloaded assistants automating via screenshot analysis). No provider-side action required there either.

### Verified against official docs (fetched 2026-06-01)

| Document | URL | Last updated (per page footer) |
|---|---|---|
| Overview of AppFunctions | https://developer.android.com/ai/appfunctions | 2026-05-12 UTC |
| Add the AppFunctions API to your app | https://developer.android.com/ai/appfunctions/add-appfunctions | 2026-05-12 UTC |
| Computer Control | https://developer.android.com/ai/computer-control | 2026-05-13 UTC |
| Agent Development Kit (ADK) for Android | https://developer.android.com/ai/adk | 2026-05-21 UTC |

### Why Gemini doesn't invoke our function — verbatim citation

From `/ai/appfunctions` (Overview), second paragraph of the body:

> **"As of May 2026, AppFunctions integration with Gemini is in a private preview with trusted testers. You can begin preparing your apps now to use AppFunctions and development tools."**

Combined with the top-of-page experimental banner:

> "**Experimental:** AppFunctions is in an experimental preview as we refine the API surface, and is subject to change."

→ As of 2026-06, third-party AppFunctions are **not** auto-discovered/invoked by the consumer Gemini app outside Google's trusted-tester allowlist. This matches §"Known Risks" row 1 of this spec (predicted at design time, now confirmed by official docs).

### `EXECUTE_APP_FUNCTIONS` permission — clarification

From `/ai/appfunctions` (Overview), body text:

> "...this lets you expose your app's capabilities as orchestratable 'tools' that authorized apps (callers) can discover and execute to fulfill user intents. **Callers must have the `EXECUTE_APP_FUNCTIONS` permission to discover and execute AppFunctions**, and can include agents, apps, and AI assistants like Gemini."

→ This is a **caller-side** permission. The Gemini app (system-preinstalled, privileged) holds it. Our Billboard app (provider) does NOT declare it and MUST NOT. On Android 16 devices, this permission has `protectionLevel="privileged"` so non-system third-party agents can't even acquire it without Instrumentation workaround or priv-app install (per `FilipFan/AppFunctionsPilot` README).

### Setup audit vs. official `/ai/appfunctions/add-appfunctions` guide

| Item | Official requirement | Our setup | Status |
|---|---|---|---|
| `compileSdk` | ≥ 36 | 36 | ✅ |
| `minSdk` | (implied 36 per AppFunctionManager requirement) | 36 | ✅ |
| KSP arg | `appfunctions:aggregateAppFunctions = "true"` | same | ✅ |
| `androidx.appfunctions:appfunctions` | required | added (alpha08 — see note) | ✅ |
| `androidx.appfunctions:appfunctions-service` | required | added | ✅ |
| `androidx.appfunctions:appfunctions-compiler` (KSP) | required | added | ✅ |
| `AppFunctionConfiguration.Provider` on Application | required when function class uses constructor injection | `BillboardApplication` implements it + Hilt provides `BillboardFunctions` | ✅ |
| `@AppFunction` annotation | required | applied to `getCurrentHot100TopSong` | ✅ |
| `@AppFunctionSerializable` on data class | required for non-primitive params/return | applied to `TopSong` | ✅ |
| `@AppFunctionSchemaDefinition` interface | **NOT mentioned as required** in official guide (only appears in third-party samples like `AppFunctionsPilot`) | not used | ✅ (matches official) |
| `<property android:name="android.app.appfunctions.app_metadata">` | **NOT mentioned** in official guide | not declared | ✅ (matches official) |
| Manifest `<service>` entries | **auto-generated by KSP compiler** — not manually declared | KSP generated `PlatformAppFunctionService` + `ExtensionAppFunctionService` (confirmed in merged manifest) | ✅ |
| `BIND_APP_FUNCTION_SERVICE` permission on services | auto-applied by KSP | confirmed | ✅ |
| `EXECUTE_APP_FUNCTIONS` permission | **caller-side only — provider must NOT declare** | not declared | ✅ |

Conclusion: **Our setup is a one-to-one match with the official guide.** No item missing.

> Note on alpha08 vs alpha09: spec originally targeted `1.0.0-alpha09` but `5dd88b4` pinned to `alpha08` after a JVM-incompatible test surfaced. Both versions are listed by the official guide as supported; alpha08 is the version `AppFunctionsPilot` ships against. No functional difference for this spike.

### What this means for the home Gemini test

Family test on Galaxy S25 with the consumer Gemini app: Gemini either answered from training data or declined. This is **expected** under "private preview with trusted testers" gating — not a defect on our side. The `dumpsys app_function` evidence (spec §"Verification Results — Task 5") proves the OS has indexed our function correctly; the consumer Gemini app simply has no policy path to invoke it for non-allowlisted publishers as of 2026-06.

### Available verification paths going forward

| Path | Description | Realism |
|---|---|---|
| A. Android Studio Gemini (official-recommended) | `/ai/appfunctions/add-appfunctions` "Verify" section says: feed Gemini in Android Studio the prompt *"Execute `adb shell cmd app_function` to learn how the tool works, then act as a chat agent aiming to invoke AppFunctions to fulfil user prompts for this app."* | Highest — official path, no extra code |
| B. Build our own caller (agent) app | Add a `:agent` module mirroring `FilipFan/AppFunctionsPilot/agent` — uses `AppFunctionManagerCompat` + workaround for the privileged `EXECUTE_APP_FUNCTIONS` permission (Instrumentation script or priv-app install) | Definitive — but largest scope |
| C. Apply for AppFunctions trusted-tester program | Google form / direct contact | Slow, external dependency |
| D. Samsung Bixby / OneUI explicit invocation | Try "Use Billboard app to find the #1 song" wording on Bixby | OEM-dependent, unverified |

### Disposition of this spike

- **Do not merge `minSdk=36` bump to `develop` / `main`** until Google moves AppFunctions out of private preview (or we accept the user breakage in exchange for an opted-in agent integration).
- Keep `feature/appfunctions-spike` branch as documented evidence that our integration is correct and ready to flip on when policy allows.
- Recommended next step before closing the spike: execute path A (Android Studio Gemini verify) — confirms end-to-end invocation works under the one officially supported path today.

---

## Verification Playbook (post-Final-Outcome)

Three verify paths in increasing effort. Run them in order — stop at the first one that succeeds.

### Path A — Android Studio Gemini (official-recommended)

Source: `/ai/appfunctions/add-appfunctions` § "Verify AppFunction integration", verbatim:
> "Use `adb shell cmd app_function list-app-functions | grep --after-context 10 $myPackageName` to see details of the AppFunctions your app provides. In Gemini in Android Studio, or other agents of your choice, provide a prompt such as the following. *Execute `adb shell cmd app_function` to learn how the tool works, then act as a chat agent aiming to invoke AppFunctions to fulfil user prompts for this app. Rely on the AppFunction description as instructions.*"

#### Steps (user-driven, requires Android Studio with Gemini enabled)

1. Connect Galaxy S25 via USB, confirm `adb devices` lists it.
2. Install our `prodDebug` build:
   ```bash
   ./gradlew :app:installProdDebug
   ```
3. Open Android Studio → enable Gemini chat panel (sidebar).
4. Paste this prompt verbatim into Gemini chat:
   > Execute `adb shell cmd app_function` to learn how the tool works, then act as a chat agent aiming to invoke AppFunctions to fulfil this user prompt for the `com.hancekim.billboard` app: "What is the current #1 song on Billboard Hot 100?". Rely on the AppFunction description as instructions.
5. Expected: Gemini in Android Studio runs an `adb shell cmd app_function` variant (likely `invoke` or `execute`), receives our `TopSong{title, artist, rank=1}`, and renders a natural-language answer naming the current Hot 100 #1.

#### Fallback if `cmd app_function list-app-functions` reports "No shell command implementation" (already observed on OneUI 8.5 — see Verification Results Task 5)

Use these alternate commands and feed Gemini in Android Studio the same prompt; it should adapt:

```bash
# Confirm package indexed with full metadata (already proven working — see Task 5 output)
adb shell dumpsys app_function | grep -A 30 com.hancekim.billboard

# Try direct invoke (command surface may vary by OneUI patch level)
adb shell cmd app_function help
```

If Android Studio Gemini cannot find an invoke sub-command on the device's `cmd app_function` surface, that confirms the device's CLI surface is incomplete and we must move to Path B.

#### Pass / Partial / Fail definitions

| Outcome | Meaning | Action |
|---|---|---|
| ✅ Pass | Gemini in AS returns the live Hot 100 #1 song title + artist | Append outcome to this spec, close spike |
| ⚠️ Partial | Gemini in AS finds the function via `list-app-functions` but cannot invoke it (no `invoke` sub-command on this device) | Move to Path B |
| ❌ Fail | Gemini in AS cannot find the function at all | Re-check `dumpsys app_function` output; if metadata is still present, it confirms the CLI surface gap, not a code defect — move to Path B |

### Path B — Build our own caller (`:agent` test module)

Used only if Path A is blocked by missing `cmd app_function` invoke sub-command on the device. Mirrors the structure of `FilipFan/AppFunctionsPilot/agent`.

#### Module shape (NOT to be merged to `develop` — test-only)

```
:agent  (new Gradle module, billboard.android.application convention)
  ├── AndroidManifest.xml
  │     uses-permission: android.permission.EXECUTE_APP_FUNCTIONS
  ├── MainActivity.kt
  │     calls AppFunctionManagerCompat.getInstance(this)
  │            .executeAppFunction(
  │                ExecuteAppFunctionRequest(
  │                    targetPackageName = "com.hancekim.billboard",
  │                    functionIdentifier = "com.hancekim.billboard.appfunctions.BillboardFunctions#getCurrentHot100TopSong",
  │                ),
  │            )
  └── start_agent.sh
        Instrumentation-based privileged permission grant (see AppFunctionsPilot README)
```

#### Why this proves invocation works regardless of consumer-Gemini policy

Because we control both caller and provider, the only thing the OS gates is the `EXECUTE_APP_FUNCTIONS` permission. The Instrumentation grant in `start_agent.sh` bypasses that without root. If our `BillboardFunctions.getCurrentHot100TopSong` returns the correct `TopSong` to this test caller, invocation path is 100% confirmed; the only remaining gap to consumer Gemini becomes purely policy-side (trusted-tester allowlist), not technical.

#### Risk

- New module + new convention plugin invocation — non-trivial diff if merged.
- Recommendation: keep `:agent` on `feature/appfunctions-spike` branch only; never push to `develop`.

### Path C — Trusted-tester enrollment (out-of-band)

Reach out via the AppFunctions FAQ link on `/ai/appfunctions#faqs`. External dependency, no code work. Track as a calendar follow-up rather than blocking the spike.

### Stop conditions

- Path A passes → close spike with PASS.
- Path A partial + Path B passes → close spike with CONDITIONAL-PASS (technical complete, policy-blocked for consumer Gemini).
- Both A and B fail → reopen investigation; likely indicates an `androidx.appfunctions:1.0.0-alpha08` runtime bug. File against the library.

---

## Decision — `:agent` test module

**Recommendation: do NOT build the `:agent` module yet.** Defer until Path A produces a partial-or-fail result.

Reasoning:

| Factor | Weight | Note |
|---|---|---|
| Path A is the official-recommended verify path | ↑ | Try it first; zero new code |
| `:agent` module adds non-trivial scope | ↓ | New module + convention plugin call + Instrumentation grant script |
| `:agent` cannot be merged to `develop`/`main` | ↓ | Test-only artifact, increases branch maintenance cost |
| `:agent` definitively proves invocation works | ↑ | Only relevant if Path A is inconclusive |
| Trusted-tester gate (consumer Gemini) is policy, not code | = | No agent module will change the consumer Gemini outcome |

**Trigger to revisit:** if Path A returns "Partial" (function listed but not invokable) or "Fail" (not listed). At that point the `:agent` module becomes the cheapest way to isolate whether the runtime invocation path is broken vs. the device CLI surface is incomplete.

**If we do build it later:** scaffold under `:agent` (NOT `:feature:agent`) so it is unambiguously out of the production graph. Convention plugin reuse: `billboard.android.application` + `billboard.android.hilt`. No production module should ever depend on `:agent`.
